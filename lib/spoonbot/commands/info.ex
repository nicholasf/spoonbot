defmodule Info do
  # def info do
  #   robot_version   = Dict.get(Mix.Project.config, :version)
  #   elixir_version  = System.version
  #   receive do
  #     { sender } ->
  #       sender <- { :ok, RobotResponse.new(msg: "I am #{robot_version} of Robot, running #{elixir_version} of Elixir.") }
  #   end
  # end
end
